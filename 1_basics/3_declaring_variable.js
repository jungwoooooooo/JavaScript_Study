/**
 * Variable 선언하기
 * 
 * 1) var - 더 이상 안 씀
 * 2) let
 * 3) const
 */
var name = '조정우';
console.log(name);

var age = 25;
console.log(age);

let ive = '아이브';
console.log(ive);

/**
 * let과 var로 선언하면
 * 값을 추후에 변결 할 수 있음.
 * 꽤 좋은 기능처럼 보이지만 나중에 보면 별로 좋지 않은 기능이다. ㅋ
 */